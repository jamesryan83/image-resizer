{
  "name": "image-resizer",
  "version": "0.1.0",
  "private": true,
  "author": "james",
  "license": "UNLICENCED",
  "description": "image resizing service.  Connects to blob storage",
  "engines": {
    "node": "10.14.1"
  },
  "scripts": {
    "dev": "set NO_DOTENV=true&& set NODE_ENV=dev&& func host start",
    "test": "set NO_DOTENV=true&& set NODE_ENV=test&& node tests/test-start.js || cd .",
    "copy:shared-files": "gulp copy-shared-files"
  },
  "dependencies": {
    "azure-storage": "^2.10.2",
    "jimp": "^0.6.0"
  },
  "devDependencies": {
    "gulp": "^4.0.0",
    "mocha": "^5.2.0"
  }
}
